export type CartItem = {
  cartId: string;
  productId: string;
  quantity: number;
};

export type CartItemDetails = {
  cartId: string;
  productId: string;
  quantity: number;
  productName: string;
  price: number;
};
